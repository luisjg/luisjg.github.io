name: Building
on:
  push:
    branches:
      - master
jobs:
  clone:
    runs-on: unbuntu-latest
    steps:
        - uses: actions/checkout@master
          with:
            fetch-depth: 1
        - run: ls
  install:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/setup-node@v1
        - run: ls

name: CI

on:
  push:
    branches:
      - master
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
      - uses: actions/checkout@v1
        run: |
          ls -l
  build:
    steps:
      - name: build
      - uses: actions/node-setup@Master
        run: |
          npm -v
          cd vue-redesign
          npm install
